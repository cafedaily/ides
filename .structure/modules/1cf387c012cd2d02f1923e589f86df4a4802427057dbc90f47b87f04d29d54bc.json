{
  "schema_version": 2,
  "module": "frontend",
  "truths": [
    {
      "statement": "Fresh startup contains no demo data or bundled model. A user creates an empty independent local space, configures their own OpenAI-compatible model, then creates ideas."
    },
    {
      "statement": "IndexedDB stores all space content and model keys on the current device. Navigation-only changes do not create revisions; atomic local revisions protect against concurrent-window overwrites and drafts remain in sessionStorage."
    },
    {
      "statement": "Model calls go directly to the configured provider with CORS, omitted cookies, no referrer and no redirects. No frontend /api calls or implicit server model fallback exist."
    },
    {
      "statement": "Settings use explicit model saves/tests, default and per-action model selection, local JSONL import/export and responsive light/dark layouts. Plain exports redact keys; encrypted exports include them."
    },
    {
      "statement": "Lexical bridges and paths are calculated in local_graph.js without uploading documents. The historical Python graph benchmark is not a performance claim for this browser implementation."
    }
  ],
  "dont_assume": [
    "Do not treat local persistence as offline inference: remote model requests send selected text to the user-chosen provider.",
    "Do not delete or silently import legacy user data."
  ]
}
