{
  "schema_version": 2,
  "module": "qa",
  "truths": [
    {
      "statement": "tests/test_chat.py covers default first-model selection with no action or route."
    },
    {
      "statement": "tests/test_chat.py covers ask and name actions routing to distinct configured model IDs using a monkeypatched urlopen rather than network."
    },
    {
      "statement": "tests/test_chat.py covers existing ask, angle, collide and rewrite route keys."
    },
    {
      "statement": "tests/test_chat.py verifies invalid configured model IDs return a clear error and do not make an outbound request."
    },
    {
      "statement": "development_check project-tests passed with 99 passing tests, including 5 chat routing tests; development_check web-syntax passed."
    }
  ],
  "dont_assume": [
    "Subagent-reported validation is not evidence unless parent invokes development_check.",
    "Chat routing tests do not contact real model services or expose credentials."
  ]
}
