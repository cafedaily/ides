{
  "schema_version": 2,
  "module": "qa",
  "truths": [
    {
      "statement": "tests/test_streaming.py uses ephemeral-port HTTP servers to prove the first downstream SSE delta arrives before a controlled upstream completion barrier is released."
    },
    {
      "statement": "Streaming regressions cover real server /api/chat SSE headers, upstream stream:true payloads, auth/origin rejection before upstream contact, invalid input, malformed upstream frames and sanitized upstream error frames."
    },
    {
      "statement": "Security regressions cover production Secure-cookie override hardening, unsupported Transfer-Encoding rejection, invalid Content-Length rejection and connection-close behavior after framing errors."
    },
    {
      "statement": "Required validation runners passed: project-tests reported 117 passed and 0 failed; web-syntax reported Web source syntax and top-level symbols passed."
    },
    {
      "statement": "128 Python tests passed including encrypted Python/JavaScript interoperability, sync atomicity, graph equivalence, security and upstream SSE."
    },
    {
      "statement": "Browser fixtures use synthetic credentials and a local mock model; actual real-model development is documented separately."
    },
    {
      "statement": "300/500-document store/API benchmark passed below 30% of full recomputation with exact output equality."
    }
  ],
  "dont_assume": [
    "An incidental socket reset traceback during the connection-close regression is not a test failure when project-tests exits 0.",
    "Do not count subagent isolated changes as applied unless explicitly accepted; the worker result was inspected and discarded in favor of parent-integrated changes."
  ]
}
