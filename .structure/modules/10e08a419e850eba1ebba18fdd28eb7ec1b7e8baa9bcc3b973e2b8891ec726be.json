{
  "schema_version": 2,
  "module": "backend",
  "truths": [
    {
      "statement": "Legacy Python API and CLI remain available as explicit migration and regression tools; the current browser application does not call them."
    },
    {
      "statement": "No historical server-side model configuration is inherited by a newly created local browser space."
    }
  ],
  "dont_assume": [
    "Do not treat local persistence as offline inference: remote model requests send selected text to the user-chosen provider.",
    "Do not delete or silently import legacy user data."
  ]
}
