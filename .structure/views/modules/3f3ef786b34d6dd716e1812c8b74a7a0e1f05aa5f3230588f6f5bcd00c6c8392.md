# qa

Generated from modules/3f3ef786b34d6dd716e1812c8b74a7a0e1f05aa5f3230588f6f5bcd00c6c8392.json.

## Current truths

- tests/test_security.py uses synthetic credentials and ephemeral local HTTP servers to cover production token refusal, anonymous/public health, unauthorized private routes, login/session/logout, bearer and cookie flows, origin rejection, key redaction/preserve/clear behavior, plain export key stripping, malformed Content-Length rejection, contained static paths, non-loopback startup refusal, and legacy API health counts.
- Current validation evidence for T-003: development_check project-tests passed with 110 tests passing; development_check web-syntax passed.

## Don't assume

- Security tests use real production credentials; all tokens and origins in tests are synthetic.
- Passing backend tests means frontend authentication UI is implemented; that is a later task.
